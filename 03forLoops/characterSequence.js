function characterSequence (input) {
    let string = input[0];
    let characters = string.length

    for (let position = 0; position < characters; position++){
        console.log(string[position])
    }
    
}
characterSequence(["bateto shef na BNT"])